/** Deployment-time settings. These do not edit Firebase or grant permissions.
 * The teacher can choose a shorter classroom profile here before deployment;
 * a student cannot downgrade their already assigned independent shift in the UI.
 */
export const RECEPTION_SETTINGS=Object.freeze({
 assessmentProfile:'extended-eight',
 pilotMinimumGroup:5
});
