# Deployment status

The repository is being prepared for GitHub Pages. The static frontend is published from `site/` by `.github/workflows/pages.yml`.

GitHub Pages hosts static files only. The shared multi-user gradebook, protected server-side grading, file uploads, and realtime classroom sessions require a separate backend. The current Pages build keeps results in the browser until that backend is connected.
